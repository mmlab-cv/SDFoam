[build-system]
requires = [
    "setuptools>=64",
    "pybind11[global]==2.13.6",
    "glfw==2.6.5",
    "cmake_build_extension",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 80
